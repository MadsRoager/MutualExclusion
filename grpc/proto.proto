syntax = "proto3";

package MutualExclusion;
option go_package = "grpc/proto";


service Ping {
    rpc ping (Request) returns (Reply) {}
}

message Request {
    int32 id = 1;
}

message Reply {
    int32 amount = 1;
}